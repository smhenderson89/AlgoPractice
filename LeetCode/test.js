// string replace example

// s = "A*b$C#"
// sLower = s.toLowerCase();
// clean = sLower.replace(/[^0-9a-z]/gi,'')
// console.log(clean);

// sort example for strings

s = "anagram"
s = s.split("").sort().join("")
console.log(s);